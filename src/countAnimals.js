const data = require('../data/zoo_data');

function countAnimals(animal) {
  // seu código aqui
  if (animal === undefined) {
    // const catchAllAnimals = (data.species.name): data.species.popularity 
    const catchAnimals = data.species.forEach((line) => {
      
    })
  }
}

module.exports = countAnimals;
